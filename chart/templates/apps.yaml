{{- /* The six app workloads. Each renders a Deployment (+ Service) via templates/_app.tpl. */ -}}
{{- range $name, $app := .Values.apps }}
---
{{ include "platform.app" (dict "name" $name "app" $app "root" $) }}
{{- end }}
