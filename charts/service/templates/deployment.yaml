{{- /*
One platform service: a Deployment (and, unless service:false, a Service), rendered by the shared
platform.app helper from platform-lib. The service supplies its own values via deploy/values.yaml —
name, image, port, probe(s), env, initContainers, volumeMounts/volumes. Shared boilerplate
(securityContext, /tmp mount, labels) stays in the helper.
*/ -}}
{{ include "platform.app" (dict "name" .Values.name "app" .Values "root" $) }}
